spring:
  config:
    import:
      - nacos:common.yml
      - nacos:datasource-forgex-test.yml
      - nacos:redis.yml
      - nacos:rocketmq.yml
      - nacos:sa-token.yml
      - nacos:captcha.yaml
  cloud:
    nacos:
      discovery:
        server-addr: ${FORGEX_NACOS_ADDR:127.0.0.1:8848}
        namespace: test
        group: DEFAULT_GROUP
      config:
        server-addr: ${FORGEX_NACOS_ADDR:127.0.0.1:8848}
        namespace: test
        group: DEFAULT_GROUP
        file-extension: yml

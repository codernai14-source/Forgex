package ${ctx.packageName}.domain.param;

import lombok.Data;

@Data
public class ${ctx.subEntityName}SaveParam {
    private Long id;
    private Long ${ctx.entityNameLower}Id;
<% for(column in ctx.subFormColumns){ %>
    private ${column.javaType} ${column.javaFieldName};
<% } %>
}
